How to compile: 
make shell

How to run: 
./shell

Implemented features and how to test
built-in commands
    - ls with no flag
    - pwd

redirection for non built-in commands
    - echo 

Not implemented

buuilt-in commands

redirection for built-in commands
    - ls
    - pwd
    - cat
    - more

Limitation
Does not work with our own file system (back end). All built-in commands used the Linux system calls, so it does not work on our own disk. 
